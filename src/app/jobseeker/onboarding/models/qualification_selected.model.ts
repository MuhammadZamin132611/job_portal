export class selectedQualification {
  // Indicates whether the Diploma qualification is selected
  show_Diploma: boolean = false;

  // Indicates whether the Doctorate qualification is selected
  show_docterate: boolean = false;

  // Indicates whether the Graduation qualification is selected
  show_Graduation: boolean = false;

  // Indicates whether the 12th Pass qualification is selected
  show_12thPass: boolean = false;

  // Indicates whether the 10th or Below qualification is selected
  show_10thBelow: boolean = false;

  // Indicates whether the Post Graduation qualification is selected
  show_postGraduation: boolean = false;

  // Indicates whether the ITI qualification is selected
  show_ITI: boolean = false;

  // Indicates whether the 10th Pass qualification is selected
  show_10thPass: boolean = false;
}
